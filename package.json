{
  "name": "cedi-loan-platform",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "serve:cedLoan": "nx serve cedLoan",
    "serve:admin": "nx serve admin",
    "serve:backend": "nx serve backend",
    "serve:all": "nx run-many --target=serve --all --parallel"
  },
  "devDependencies": {
    "nx": "^21.0.0"
  }
}
